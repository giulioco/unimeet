$('#home a[href*="/pages/show_card?id=<%=@oldId%>&method=get"]').hide('slow', function(){ $(this).remove();});
$('#card-container').html("<%= escape_javascript(render :partial => 'activities/activity_card', locals: { activity: @nextInQueue }, :remote => true) %>");
$(document).ready( function() {
$('a').on('ajax:beforeSend', function() {
  $("#loader").show();
});

$('a').bind('ajax:complete', function() {
  $("#loader").hide();
});
});